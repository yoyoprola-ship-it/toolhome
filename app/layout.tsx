import type { Metadata } from 'next';
import { Geist, Geist_Mono } from 'next/font/google';
import './globals.css';

const geistSans = Geist({
  variable: '--font-geist-sans',
  subsets: ['latin'],
});

const geistMono = Geist_Mono({
  variable: '--font-geist-mono',
  subsets: ['latin'],
});

export const metadata: Metadata = {
  title: 'ToolHome — Numbers, mailboxes, cameras. Installed.',
  description:
    'Home upgrades installed right at your door. House numbers, mailboxes, Ring cameras, and more — for homeowners in Lafayette, LA.',
  openGraph: {
    title: 'ToolHome',
    description:
      'Home upgrades installed right. Numbers, mailboxes, cameras.',
    type: 'website',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col bg-white text-slate-900">
        {children}
      </body>
    </html>
  );
}
